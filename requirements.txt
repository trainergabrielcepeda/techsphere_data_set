# Core Spark / Delta — Delta-native idioms per project convention (medallion tables, MERGE upserts)
pyspark>=3.5,<4.0
delta-spark>=3.2,<4.0
PyYAML>=6.0

# Componente 1 — generación clínica base (Diseño Técnico §5)
# Riesgo R1 materializado (§5.2, Plan 05): el pysynthea real (PyPI) es un downloader/
# query-tool sobre una base OMOP de Synthea ya generada, no un generador de pacientes —
# no cubre el caso de uso del diseño. Reemplazado por un generador sintético propio
# (generate_synthea_profiles.py), implementado solo con random/stdlib — sin dependencia
# de runtime externo.

# Componente 2 — adaptación geográfica Colombia (§6)
Faker>=25.0

# Componente 4 — orquestación de llamadas LLM (§8.2): Databricks Model Serving SDK
# (WorkspaceClient, también usado para leer secretos) y/o OpenRouter (Plan 06, primario
# — HTTP plano vía urllib/stdlib, sin SDK propio). Credenciales siempre vía Databricks
# Secret Scopes, nunca hardcodeadas.
databricks-sdk>=0.30

# Componente 4 — camino alternativo vía la API de Anthropic (Plan 08), sin la cuota
# diaria de cuenta del nivel gratuito de OpenRouter. Plan 06 había quitado esta
# dependencia cuando el camino OpenRouter (HTTP plano) la dejó como peso muerto — vuelve
# a usarse de verdad ahora en _anthropic_client.
anthropic>=0.68

# Tooling
pytest>=8.0
